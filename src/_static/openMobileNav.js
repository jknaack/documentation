function openMobileNav() {
    const mobileNavigationOverlay = document.querySelector(".mobile-navigation-overlay")
    mobileNavigationOverlay.style.display = mobileNavigationOverlay.style.display === "block" ? "none" : "block" 
}